[project]
name = "scammer4u"
version = "0.1.0"
description = "Social Engineering Attack Benchmark for Web-based VLM Agents"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "jinja2",
]
